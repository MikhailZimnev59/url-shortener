# URL Shortener

Простое приложение для сокращения ссылок на FastAPI.

## 🚀 Запуск

### Установка зависимостей
pip install -e .

### Запуск приложения
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

Приложение будет доступно по адресу: http://localhost:8000

